{
  "name": "sifapaimai",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "fetch": "node scripts/run-fetch.js",
    "fetch:test": "node scripts/run-fetch.js --test",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "playwright": "^1.54.0"
  },
  "devDependencies": {
    "vitest": "^3.2.0"
  }
}
